# Program to convert a given number in decimal format to binary, octal and hexadecimal format

num = int(input('Enter The Number : '))

print('Binary :', bin(num))
print('Octal :', oct(num))
print('Hexadecimal :', hex(num))